export class CreatePhoto {
  fileName: string;
  desc: string;
}

export class UpdatePhoto {
  id: number;
  fileName: string;
  desc: string;
}

export class QueryPhoto {
  fileName: string;
  desc: string;
}
